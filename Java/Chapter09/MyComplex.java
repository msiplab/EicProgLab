public class MyComplex {

    /* フィールド */
    double real;
    double imag;

    /* インスタンスメソッド */
    /*
    public void display() {
	char s = ( this.imag < 0 ) ? '-' : '+';
	System.out.printf("%f %c i%f\n", this.real ,s, Math.abs(this.imag));
    }
    */

    /*
    public double abs() {
	return Math.sqrt(this.real*this.real + this.imag*this.imag);	
    }
    */
}
